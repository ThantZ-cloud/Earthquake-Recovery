{
  "name": "earthquake-recovery",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:app": "cd app && npm run dev",
    "dev:api": "cd api && npm run dev",
    "install:all": "cd app && npm install && cd ../api && npm install",
    "postinstall": "cd api && npm install",
    "start": "cd api && node server.js"
  }
}
